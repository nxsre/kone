language: go

go:
    - 1.8
    - tip

script:
    - go test -race -v -bench=. github.com/FlowerWrong/kone/geoip
    - go test -race -v -bench=. github.com/FlowerWrong/kone/tcpip
    - go test -race -v -bench=. github.com/FlowerWrong/kone/k1
